// logo components with width prop use as per need 
// eslint-disable-next-line react/prop-types, no-unused-vars
const Logo = ({ width = '100px' }) => {
    return (
        <div>Logo</div>
    )
}

export default Logo